<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aniwag.mapper.MemberMapper">

	<insert id="create">
		INSERT INTO TBL_MEMBER
		(mem_id, mem_password, mem_phone, mem_gender,
		mem_name, mem_eamail, mem_addr1,
		mem_addr2, mem_addr3)
		VALUES
		(#{mem_id}, #{mem_password}, #{mem_phone}, #{mem_gender},
		#{mem_name},
		#{mem_email}, #{mem_addr1}, #{mem_addr2}, #{mem_addr3})
	</insert>

	<select id="read">
		SELECT * FROM TBL_MEMBER
		WHERE MEM_ID = #{mem_id}
	</select>

	<update id="update">
		UPDATE TBL_MEMBER
		SET mem_password = #{mem_password}, mem_phone = #{mem_phone},
		mem_gender = #{mem_gender}, mem_name = #{mem_name}, mem_email = #{mem_email}, 
		mem_addr1 =	#{mem_addr1}, mem_addr2 = #{mem_addr2}, mem_addr3 = #{mem_addr3}
		WHERE mem_id = #{mem_id}
	</update>

	<delete id="delete">
	
		DELETE FROM TBL_MEMBER
		WHERE mem_id = #{mem_id}
	
	</delete>	

</mapper>